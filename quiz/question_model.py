# Creates the Question class, and should be named in PascalCase.
class Question:
    # Constructor function to initialize attributes for created objects.
    def __init__(self, text, answer):
        self.text = text
        self.answer = answer